version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - echo "VITE_API_URL=$API_BASE_URL" > .env   # for Vite/React
      - npm run build
artifacts:
  files:
    - '**/*'
    - '.env'
  base-directory: dist
